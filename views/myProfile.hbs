<main>
  <h1 class="text-center">My Profile</h1>
  <div class="profile"><img src="./img/profile.png" alt="default user" />
    <h3>User Info:</h3>
    <div class="flex">
      <p>Username: </p>
      <p>{{user.username}}</p>
    </div>

    <div class="flex">
      <p>Enrolled Courses: </p>
      <p>
        {{#if courses}}
          <span>{{courses}}</span>
        {{else}}
          <span>You have not enrolled to any courses yet.</span>
        {{/if}}
      </p>
    </div>
  </div>

</main>